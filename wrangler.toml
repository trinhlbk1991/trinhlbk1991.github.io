name = "icedtealabs"
compatibility_date = "2024-12-01"
pages_build_output_dir = "./dist"

# Note: KV bindings for Cloudflare Pages are configured in the dashboard
# Settings > Functions > KV namespace bindings
